<Window x:Class="CommunityHub.Ui.Views.ProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Profil" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50" Padding="15">
            <Grid>
                <TextBlock x:Name="UserInfoTextBlock"
                           Foreground="White"
                           FontSize="14"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Nova objava"
                            Click="NewPostButton_Click"
                            Padding="15,8"
                            Margin="0,0,10,0"
                            FontSize="14"/>
                    <Button Content="Početna"
                            Click="HomeButton_Click"
                            Padding="15,8"
                            Margin="0,0,10,0"
                            FontSize="14"/>
                    <Button Content="Odjavi se"
                            Click="LogoutButton_Click"
                            Padding="15,8"
                            FontSize="14"/>
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="PostsItemsControl" Margin="40,20">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E0E0E0" BorderThickness="1"
                                Background="White" Margin="0,0,0,15"
                                Padding="20" CornerRadius="5">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           Foreground="#2C3E50"
                                           Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding Content}"
                                           FontSize="14"
                                           TextWrapping="Wrap"
                                           Foreground="#34495E"
                                           Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding CreatedAt, StringFormat='dd.MM.yyyy. HH:mm'}"
                                           FontSize="12"
                                           Foreground="#95A5A6"
                                           FontStyle="Italic"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
